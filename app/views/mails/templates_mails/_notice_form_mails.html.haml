- if mail.errors.any?
 #error_explanation
  %h2
   = "#{pluralize(mail.errors.count, "error")} prohibited this mail from being saved:"
  %ul
   - mail.errors.full.messages.each do |msg|
    %li
     = msg

