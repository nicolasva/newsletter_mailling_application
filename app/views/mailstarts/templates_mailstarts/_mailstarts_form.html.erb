<h2><%= t("mailstarts.edit.mails_list_start") %></h2>
<p>
	  	<%= mailstart_field.label(:name) %><br>
		<%= mailstart_field.text_field(:name) %>
	  </p>
	  <p>
	  	<%= mailstart_field.label(:mail) %><br>
		<%= mailstart_field.text_field(:mail) %>
 	  </p>


<p>
<%= mailstart_field.submit(t(".add_new_category"), :name=>"button_addcategoryall_#{@mailstart.id}") %>
</p>

